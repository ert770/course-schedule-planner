# 2026-09-17 修正 D3／D4／D6／D26–D29（過時文件與註解）

## 修改日期

2026-09-17

## 為什麼做這件事

`docs/application-portfolio/01_目前已完成/07_已知限制.md` 記錄的「文件與程式不一致」清單中，D3、D4、D6、D26–D29 這幾項本人決定直接修正（不只是記錄），逐項指示如下。

## 修改檔案清單

- `README.md`：
  - 技術棧從「React.js 或 Vue.js」「Node.js 或 Python」「MySQL 或 MongoDB」改成實際採用的 React（Vite）、Node.js（Express 5，ESM）、MySQL（Aiven，共用資料庫）（D4）。
  - 評論爬蟲段落改為說明爬蟲程式在另一個獨立 repo（組員 A 負責），不在本 repo 內，並留了連結佔位（D3，連結待補）。
- `AGENTS.md`：兩處「JSON 檔案式資料庫」改成「以 MySQL 為主，JSON 為後備」（D6）。
- `.env.example`：移除 `GEMINI_API_KEY`（D26）。
- `server/package.json`／`server/package-lock.json`：用 `npm uninstall @google/genai` 移除未使用的依賴（D26）。
- `server/src/skills/preferenceLearning.js:78-80`：刪除引用已不存在的 `PLAN_VARIANTS` 的註解（D27）。
- `server/src/services/preferenceLearningService.js:176-182`：`recomputeLearnedWeights()` 的 JSDoc 從「這輪不接進排課」改成準確描述——已接進排課（方案層），但只在呼叫本函式時才重算，排課請求本身不會觸發（D28）。
- `server/scripts/agentGoldenSetReport.js:5`：註解「12 題單輪」改成「13 題單輪」，與 `test/fixtures/agentGoldenSet.json` 的 `cases` 陣列實際長度一致（D29）。
- `docs/application-portfolio/01_目前已完成/07_已知限制.md`：
  - D3、D4、D6、D26、D27、D28、D29 因已解決，從表格中刪除。
  - D24、D25 依本人要求補上詳細說明（逐項指出程式碼實際行為，例如 `scoreCourse()` 其實會讀 `constraintSchema.js` 的內容偏好權重、Agent 工具本身就是會設定 `allowRelaxation` 的呼叫端、`planStrategies.js` 是動態產生方案而非固定五個），但**沒有修改** `SCHEDULING_LOGIC.md`／`PROMPT_DESIGN.md` 本身，本人只要求解釋，未要求修正這兩份文件。

## 測試與驗證

- 執行 `node --test test/preferenceLearning.test.js test/preferenceLearningService.test.js`：65/65 通過。
- 執行 `CI=true node --test --test-force-exit "test/**/*.test.js"`：1,058 個測試，1,052 通過，6 個失敗——3 個是既有的 Windows libuv 結束 assertion（`authRoutes`、`privacyRoutes`、`scheduleRoutes`，與本次修改無關），3 個是既有的 K25（`getMysqlCourses()` 欄位名稱與共用資料庫不符，見同一份已知限制文件），與本次修改無關，本次沒有新增任何失敗。
- **意外事件**：驗證 `agentGoldenSetReport.js` 是否仍能正常載入時，用 `import()` 直接匯入該檔案，誤觸發了整支 golden set 對真實 OpenAI API 的呼叫（該腳本沒有 `if (require.main===module)` 這類守衛，`import` 就會執行）。這產生了非預期的 API 費用，並覆寫了 `server/test/reports/golden-set-latest.json`。已立即用 `git checkout -- server/test/reports/golden-set-latest.json` 還原該檔案；程式碼與文件本身沒有受影響，但那次呼叫的 OpenAI API 費用無法收回。之後改用檢查 `.env` 依賴是否安裝、直接跑測試檔的方式驗證，不再對這類腳本用 `import()` 探測。
- `git diff --stat -- client`：空，前端沒有變動。

## 影響範圍

- `server/` 的變動只有註解與一個未使用依賴，沒有改變任何執行邏輯或 API 行為。
- `docs/project-guide/` 相關內容已在同一批工作中處理（見 [2026-09-17-remove-project-guide.md](./2026-09-17-remove-project-guide.md)）。

## 是否 commit 與 push

未 commit，等待使用者指示。
