# 2026-09-17 修正 D24／D25／D30，刪除 D5（過時文件）

## 修改日期

2026-09-17

## 為什麼做這件事

延續同一天稍早的 D3／D4／D6／D26–D29 修正批次，本人指示把 D5 刪除、把 D24／D25／D30 對應的來源文件實際更新。

## 修改檔案清單

- `docs/SCHEDULING_LOGIC.md`：
  - `constraints.allowRelaxation` 的說明從「沒有任何呼叫端會設定」改成準確描述——AI Agent（`promptService.js` 的 `run_csp_scheduler` 工具參數）目前是唯一會設定它的呼叫端，一般表單路徑仍維持預設 `false`（D24 之一）。
  - Bounded backtracking repair 段落把「系統保留五個既有 greedy variant」改成「依使用者已表態的偏好軸數量動態決定，1 到 5 個」，與 `planStrategies.js` 的 `buildPlanStrategies()` 一致（D24 之二）。
  - `scoreCourse()` 讀不讀 schema 權重（D24 第三項）：檢查全文後，現有內容（:552、:574-575）已經正確描述 `scoreCourse()` 會套用 `constraintSchema.js` 定義的內容偏好權重，沒有找到需要修正的錯誤敘述，本次未變更相關段落。
- `docs/PROMPT_DESIGN.md:588-593`：few-shot 範例的 `function_call` 補上必填的 `interpretation` 欄位（含 `nonNegotiable`／`flexible`／`creditGoal`／`notMentioned`），並移除使用者沒有明講的 `minCredits`／`maxCredits`（D25）。
- `docs/resume-fact-check.md` §3 #4：`mustTakeCourses`／`avoidInstructors` 的敘述從「未持久化」改成「已持久化」，附上 `database.js:682-687`（讀）與 `database.js:885-896`（寫）的依據，並移除原本指向已刪除的 `docs/project-guide/11-known-limitations.md` 的引用（D30）。
- `docs/application-portfolio/01_目前已完成/07_已知限制.md`：D5（`report/期中專題進度報告.md` 的 CSP／100 毫秒宣稱）依指示直接刪除，不修改 `report/期中專題進度報告.md` 本身——那是一份有日期的歷史報告，代表撰寫當下的認知，修改它等於竄改歷史，跟 README.md／AGENTS.md 這類「活文件」的處理方式不同。D24、D25、D30 因為來源文件已修正，一併刪除。
- `docs/application-portfolio/05_推甄前預計完成/00_Roadmap.md`、`02_P1加分工作.md`：P2-2 更新為「只剩 D1、D2 待處理」。
- `docs/application-portfolio/08_個人貢獻證明/04_設計文件.md`：`SCHEDULING_LOGIC.md`／`PROMPT_DESIGN.md`／`resume-fact-check.md` 三列的狀態欄更新為「已修正」。
- `docs/application-portfolio/10_專題完整報告/專題完整報告.md`：「文件與程式不一致」統計從剩 6 項更新為剩 2 項（D1、D2）。

## 測試與驗證

- 全部是文件編輯，沒有修改任何 `server/`、`client/` 底下的功能程式碼，本批次不需要重跑測試。
- 用 Grep 對 `docs/application-portfolio/` 全文搜尋 `\bD5\b`、`\bD24\b`、`\bD25\b`、`\bD30\b`，確認除了本次新增的說明文字外沒有遺漏的舊引用。
- `git status` 確認變更僅限於 `docs/SCHEDULING_LOGIC.md`、`docs/PROMPT_DESIGN.md`、`docs/resume-fact-check.md` 與 `docs/application-portfolio/` 內的檔案；`git diff --stat -- server client` 為空。

## 是否 commit 與 push

未 commit，等待使用者指示。
